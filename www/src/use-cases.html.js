import { htmlTemplate, socialMetadata } from './_htmlTemplate.js'
import { shell } from './_syntaxHighlight.js'

// language=html
export default (url) => htmlTemplate({
	head: socialMetadata('Use Cases', '', url),
	body: `
		<h1>Use cases</h1>

		<h2>Working around unstable dev backends</h2>
		<p>
			If you are a frontend developer you might be
			spending too much time syncing the database and spinning up dev
			infrastructure. And sometimes blocked by its instability.
		</p>


		<h2>Develop ahead of backend</h2>
		<p>
			Sometimes frontend developement could be blocked by waiting for backend APIs to be ready.
			See <a href="https://github.com/ericfortis/mockaton/blob/main/demo-app-vite/src/ColorModel.js">ColorModel.js</a>
			for an example of how you can decouple your frontend code from the API contract,
			so API changes become a matter of updating only one place.
		</p>


		<h2>All permutations</h2>
		<p>
			Besides helping to set up screenshot diffing tests
			(see <a href="https://github.com/ericfortis/mockaton/tree/main/pixaton-tests">pixaton-tests/</a>
			in the repo) developing against comprehensive states help to spot inadvertent regressions.
			For example, see these ColorCards below.
		</p>

		<div class="RegionImg" data-max-width="740">
			<img src="/assets/media/demo-app.avif" alt="Mockaton Demo App Screenshot" />
		</div>


		<h2>Time travel</h2>
		<p>
			If you commit the mocks to the repo, you won’t need to downgrade
			backends when checking out long-lived branches or bisecting bugs.
		</p>


		<h2>Standalone demo server</h2>
		<p>
			You can deploy a Mockaton instance with your built SPA.
			For that, put the built assets in <code>config.mocksDir</code>.
		</p>
		<p>
			The repo includes a demo which builds the frontend app and runs
			a docker container with Mockaton as backend.
		</p>

		${shell`
git clone https://github.com/ericfortis/mockaton.git --depth 1
cd mockaton/demo-app-vite
make run-standalone-demo
`}

		<p>App: <a href="http://localhost:4040">http://localhost:4040</a></p>
		<p>Dashboard: <a href="http://localhost:4040/mockaton">http://localhost:4040/mockaton</a></p>

		<br/>
		<h2>Testing</h2>
		<p>
			In summary, Mockaton lets you test flows that are
			difficult to reproduce from an actual backend.
		</p>

		<h3>Errors and edge cases on third-party APIs</h3>
		<p>
			Trigger states that might otherwise be impossible to test when using third-party APIs.
		</p>

		<h3>Recovering from an error</h3>
		<p>
			For instance, on a design review, you might need to demo what
			happens when API recovers from an error. So you could start
			with an autogenerated <code>500</code> response, and then toggling
			it OFF to a <code>200</code>.
		</p>


		<h3>Slow to generate resources</h3>
		<p>
			Similar to testing error recovery, when polling APIs, say, a slow
			to generate backend resource. Begin lets say with a <code>204</code>
			(No Content) mock, and then switch it over to a <code>200</code>.
		</p>


		<h3>Sensitive data APIs and resources</h3>
		<p>
			Say you need to work with sensitive documents you are not
			allowed to see, such as PII (Personally Identifiable Information).
		</p>

	`
})
